// diff — pixelmatch + SSIM + silhouette. Implemented in M3 (T31-T34).

const NOT_READY = 'evth: diff runtime is wired in milestone M3 (tasks T31-T34).'

export function diffImages() {
  throw new Error(NOT_READY)
}

export function makeDiffPng() {
  throw new Error(NOT_READY)
}
